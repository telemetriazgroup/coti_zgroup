# bullmq@5.x declara peerOptional redis>=5; este repo usa redis@4 (caché de catálogo).
# BullMQ habla con Redis vía ioredis, no vía el paquete `redis`. Sin esto, npm 10
# en Docker (node:20-alpine) falla ERESOLVE al no haber package-lock.json.
legacy-peer-deps=true
